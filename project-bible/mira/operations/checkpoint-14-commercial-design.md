# CP14 — reconciled commercial launch surfaces

2026-09-17. Three additional original Russian funnels `/mira/go/`, `/mira/practical/`, `/mira/corporate/`, compared at `/mira/launch/`. Direct red door / blue working task / green corporate roles. Local no-PII qualification and optional lightweight motion. Existing editorial, agency, illustrated marketplace and all concurrent start/growth/business/variants/phuket/expo routes are preserved.

The concurrently published `/mira/business/` is not overwritten: `mira-build-campaign.py` is an explicit route registry over byte-preserved layout source. No force push or loss of the parallel deployment/release tests. Both public catalogue skins expose the same618research records, not available units.

Original vector banner at `/mira/exhibition/` uses exact QR to `https://wegc.fund/mira/go/`. Working1920x1080digital and85x200cmrollup PDF/PNG/SVG. All three local PNGs decoded to the exact URL. Image-generation service did not start; final artwork is original vector geometry. No competitor photographs, code, logos or font files copied.

Local combined source regression:83Node and102Python tests passed. Earlier3variant×7width offline checks passed; renamed route preserves same layout. Full live browser verification of these added routes follows. Existing parallel release run35196047440 was inspected: actual domain browser12casegroups, no page/CSP errors, 80GET/concurrency4 all200, p50 36.87ms/p95 124.26ms in that run. These are measurements for the prior released routes, NOT the new CP14 routes or a server capacity test. Its real registration flag is false.

Observed classic Pages deployment35195901549 failed because another deployment was in progress; public byte/browser checks subsequently passed. To avoid serving incomplete generated surfaces from a second publishing path, QA now saves all generated public documents/pages/banner files to the explicit public paths. No private evidence is added.

Owner/legal/design/print approval and real customer-data architecture remain open. No live registration, external messages or developer forms are performed. Continue actual CP14 HTTP/CSP verification, controlled negative cases and link/QR tests; publish the measured report after success.
